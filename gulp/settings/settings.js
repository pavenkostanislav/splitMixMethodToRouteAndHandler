/*jshint esversion: 6 */

const buildPath = './build';

const settings = {
  srcPath: './src',
  testPath: './spec',
  buildPath,
  srcBuildPath: buildPath + '/src',
  testBuildPath: buildPath + '/spec',
};

module.exports = settings;
